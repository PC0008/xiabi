# 生产验收与配置清单 v1

更新时间：2026-05-27

## 线上地址

- 用户端 H5：`https://immortal-sponge-1728.edgespark.app/index.html`
- 管理后台：`https://immortal-sponge-1728.edgespark.app/admin.html`
- 健康检查：`https://immortal-sponge-1728.edgespark.app/api/public/health`

## 已具备的真实能力

- 用户端首页、通话引导、打字模式、生成页、信件页、记录页、订单页、设置页均可交互。
- 后台首页文案、通话引导、销售信模板、价格权益、支付开关会真实影响用户端。
- 写信生成走服务端 DeepSeek；DeepSeek 不可用时任务会失败，不再生成本地兜底信件。
- 首次免费、单封解锁、年卡权益都写入权益流水；前端不能直接决定权限。
- 单封解锁必须关联当前会话的一封销售信。
- 导出打印版前会校验领取、单封或年卡权益。
- 微信 H5 支付下单、支付回调验签/解密、订单置 paid、权益发放、后台查单和补发权益已接入。
- 阿里云短信发送、验证码校验、手机号绑定已接入；短信未配置时会明确失败。
- MiniMax TTS 已接入说话能力；用户端语音输入优先使用浏览器语音识别，不支持时可录音提交到服务端 ASR 接入槽。
- 后台有用户、信件、任务、订单、权益、支付回调、审计日志列表和详情；失败任务可重试。

## 必填配置

### DeepSeek

- `LETTER_PROVIDER=deepseek`
- `DEEPSEEK_MODEL=deepseek-v4-pro`
- `DEEPSEEK_BASE_URL=https://api.deepseek.com`
- Secret：`DEEPSEEK_API_KEY`

### MiniMax 说话

- `VOICE_PROVIDER=minimax`
- `MINIMAX_VOICE_ID=moss_audio_7a15101b-4f96-11f1-b3fb-821d30892425`
- Secret：`VOICE_API_KEY`

说明：MiniMax 官方 API 总览当前公开列出的 Speech 能力是 T2A、T2A Async、Voice Cloning、Voice Design、Voice Management，未列出独立 STT/ASR 转写接口。语音输入如果必须走 MiniMax，需要 MiniMax ASR/转写接口文档或后台开通说明。

### 语音输入转写接入槽

- `VOICE_ASR_PROVIDER`
- `VOICE_ASR_ENDPOINT`
- `VOICE_ASR_MODEL`
- Secret：`VOICE_ASR_API_KEY`

说明：浏览器不支持直接语音识别时，用户端会录音提交到 `/api/public/voice/transcribe`。服务端会把录音提交给 `VOICE_ASR_ENDPOINT`，并从返回值里的 `transcript`、`text`、`data.transcript`、`data.text`、`result.transcript` 或 `result.text` 读取识别文本。未配置时会明确提示切换打字模式，不会伪造识别结果。

### 微信支付 H5

- `PAYMENT_PROVIDER=wechat`
- `WECHAT_PAY_APP_ID`
- `WECHAT_PAY_MCH_ID`
- `PAYMENT_NOTIFY_URL=https://immortal-sponge-1728.edgespark.app/api/webhooks/wechat-pay`
- `PUBLIC_BASE_URL=https://immortal-sponge-1728.edgespark.app`
- Secret：`WECHAT_PAY_PRIVATE_KEY`
- Secret：`WECHAT_PAY_CERT_SERIAL_NO`
- Secret：`WECHAT_PAY_API_V3_KEY`
- Secret：`WECHAT_PAY_PLATFORM_PUBLIC_KEY`

说明：普通手机浏览器走 H5 支付；微信内打开通常需要 JSAPI + openid 授权链路，需单独确认。

### 阿里云短信

- `SMS_PROVIDER=aliyun`
- `SMS_ALIYUN_SIGN_NAME`
- `SMS_ALIYUN_TEMPLATE_CODE`
- Secret：`SMS_API_KEY`
- Secret：`SMS_API_SECRET`

## 生产验收步骤

1. 后台登录后，保存首页文案、引导阶段、销售信模板和价格配置。
2. 用户端打开首页，确认后台文案已同步。
3. 完成一次打字或语音识别通话，生成销售信。
4. 绑定真实手机号，确认短信能收到且验证码可绑定。
5. 领取首次免费信件，确认权益流水出现 `first_free_letter`。
6. 再生成一封信，确认不能重复首次免费领取。
7. 创建单封解锁订单，完成微信支付，确认回调后订单为 `paid`，权益流水出现 `single`。
8. 创建年卡订单，完成微信支付，确认权益流水出现 `annual`。
9. 订单回调延迟时，在用户端订单页点“刷新支付结果”，在后台订单详情点“查单”。
10. 对已支付但权益缺失的订单，在后台点“补权益”，确认幂等不重复发放。
11. 打开信件导出，确认未领取/未解锁不能导出，已领取/已解锁/年卡可打开打印版。
12. 在后台查看用户、信件、任务、订单、权益、支付回调和审计日志详情。

## 当前仍需外部确认

- 真实微信小额支付必须由商户号完成一次真实付款和回调验收。
- 真实短信必须用真实手机号验收一次，避免只验证接口结构。
- MiniMax 官方公开文档没有列出独立 ASR 接口；如必须服务端转写，请配置可用的 `VOICE_ASR_ENDPOINT`，或补充 MiniMax 对应接口文档后再精确适配。
- 服务端直出二进制 PDF 尚未接入；当前是可打印 HTML，用户可在浏览器保存为 PDF。
