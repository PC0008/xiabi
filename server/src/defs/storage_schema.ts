import type { BucketDef } from "@sdk/server-types";

export const xiabiFiles: BucketDef<"xiabi-files"> = {
  bucket_name: "xiabi-files",
  description: "Sales letters, exports, voice uploads, and generated files."
};
